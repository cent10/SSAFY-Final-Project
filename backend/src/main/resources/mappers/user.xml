<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.activityx.allei.dao.UserDao">
	<!-- create -->
	<insert id="create">
        insert into user(
            email, name, key
        ) values (
            #{email}, #{name}, #{key}
        )
	</insert>

	<!-- readAll -->
    <select id="readAll" resultType="User">
        select id, email, name, domain, key
        from user
    </select>
    
	<!-- findById -->
	<!-- findByName -->
	<!-- findlistByName -->
	<!-- findByEmail -->
	<!-- findlistByEmail -->
	<!-- findByKey -->
	<!-- update -->
	<!-- delete -->

</mapper>